//constant are use to ddeclare values that are not changeable 
const a =42;
//tis value cant be changed
//let is used witin the scope of the code
let cardId = 100;
console.log(cardId);
//will only work on the scope it is declared
if (true){
	let cardId =200
}
console.log(carId);
//will not work because of yhe let 
//but using the var keyword will work in the above code
//REST PARAMETERS